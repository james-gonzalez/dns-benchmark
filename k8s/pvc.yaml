apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dns-bench-results
  namespace: dns-benchmark
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn-rwx
